import React, {
    FunctionComponent,
    ReactElement,
} from 'react';
import {
    useStaticQuery,
    graphql,
} from 'gatsby';
import * as styles from './BlogPosts.module.scss';

export const BlogPosts: FunctionComponent = (): ReactElement => {
    return (
        <section>

        </section>
    );
}
